<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="DoctorMemberAssoMapper">
	<resultMap type="DoctorMemberAsso" id="doctorMemberAssoResultMap">
		<id column="id" property="id"/>
		<result column="doctor_id" property="doctorId"/>
		<result column="member_id" property="memberId"/>
		<result column="del_flag" property="delFlag"/>
		<result column="create_user_id" property="createUserId"/>
		<result column="create_time" property="createTime"/>
		<result column="update_user_id" property="updateUserId"/>
		<result column="update_time" property="updateTime"/>
	</resultMap>
	
	<!-- 新增-->
	<insert id="save" parameterType="DoctorMemberAsso" useGeneratedKeys="true" 
		keyProperty="id">
		insert into p_doctor_member_asso(
			doctor_id,
			member_id,
			create_user_id,
			create_time
		) values (
			#{doctorId},
			#{memberId},
			#{createUserId},
			#{createTime}
		)
	</insert>
	
	<!-- 删除-->
	<delete id="delete" parameterType="java.lang.Long">
		delete from p_doctor_member_asso
		where
				id = #{value}
	</delete>
	
	<!-- 软删除-->
	<update id="deleteLogical" parameterType="java.lang.Long">
		update p_doctor_member_asso set del_flag=1
		where id = #{value}
	</update>

	<!-- 修改 -->
	<update id="update" parameterType="DoctorMemberAsso">
		update p_doctor_member_asso
			set 
			<if test="doctorId!=null">
				`doctor_id` = #{doctorId},
			</if>
			<if test="memberId!=null">
				`member_id` = #{memberId},
			</if>
		<if test="updateUserId!=null and updateUserId!=''">
			update_user_id = #{updateUserId},
		</if>
		<if test="updateTime!=null">
			update_time = #{updateTime},
		</if>
			id = id
			where
				id = #{id}
	</update>
	
	
	<!-- 通过id获取数据 -->
	<select id="findById" parameterType="java.lang.Long" resultMap="doctorMemberAssoResultMap">
		select * from 
			p_doctor_member_asso
		where 
			id = #{value}
	</select>
	
	<!-- 条件查询 -->
	<select id="findByCondition" parameterType="DoctorMemberAsso" resultMap="doctorMemberAssoResultMap">
		select * from 
			p_doctor_member_asso where del_flag=0
			<if test="doctorId!=null">
				and `doctor_id` = #{doctorId}
			</if>
			<if test="memberId!=null">
				and `member_id` = #{memberId}
			</if>
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultMap="doctorMemberAssoResultMap">
		select * from 
				p_doctor_member_asso a where del_flag=0
		<if test="pd.query_time_start!=null and pd.query_time_start!=''"><!-- 时间检索 -->
			and a.create_time &gt;= #{pd.query_time_start}
		</if>
		<if test="pd.query_time_end!=null and pd.query_time_end!=''"><!-- 时间检索 -->
			and a.create_time &lt;= #{pd.query_time_end}
		</if>	
		<if test="pd.query_key!=null and pd.query_key!=''"><!-- 关键字检索 -->
			and a.name like concat('%',#{pd.query_key},'%')
		</if>
			order by doctor_id desc
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="String" resultMap="doctorMemberAssoResultMap">
		select * from p_doctor_member_asso a
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteBatch" parameterType="String">
		delete from p_doctor_member_asso
		where id in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<!-- 批量删除 -->
	<update id="deleteLogicalBatch" parameterType="String">
		update p_doctor_member_asso set del_flag=1  
		where 
			id in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</update>
	
</mapper>