<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="CheckSheetClassifyMapper">
	<resultMap type="CheckSheetClassify" id="checkSheetClassifyResultMap">
		<id column="id" property="id"/>
		<result column="name" property="name"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="create_time" property="createTime"/>
	</resultMap>
	
	<!-- 新增-->
	<insert id="save" parameterType="CheckSheetClassify" useGeneratedKeys="true" keyProperty="id">
		insert into check_sheet_classify(
        name,
        create_user_id,
        create_time
		) values (
            #{name},
        #{createUserId},
        #{createTime}
		)
	</insert>

    <!--批量新增-->
    <insert id="batchSave" parameterType="list">
        insert into check_sheet_classify(
            name,
            create_user_id,
            create_time
        )
        values
        <foreach collection ="list" item="item" index= "index" separator =",">
        (
            #{item.name},
            #{item.createUserId},
            #{item.createTime}
        )
        </foreach>
    </insert>

	<!-- 删除-->
	<update id="deleteLogical" parameterType="java.lang.Long">
		delete from check_sheet_classify
		where 
			id = #{value}
	</update>

	<!-- 修改 -->
	<update id="edit" parameterType="CheckSheetClassify">
		update check_sheet_classify
			set 
			<if test="name!=null and name!=''">
				name = #{name},
			</if>
			id = id
			where 
				id = #{id}
	</update>
	
	
	<!-- 通过id获取数据 -->
	<select id="findById" parameterType="java.lang.Long" resultMap="checkSheetClassifyResultMap">
		select * from 
			check_sheet_classify
		where 
			id = #{value}
	</select>
	
	<!-- 条件查询 -->
	<select id="findByCondition" parameterType="CheckSheetClassify" resultMap="checkSheetClassifyResultMap">
		select * from 
			check_sheet_classify where 1=1
			<if test="createUserId!=null and createUserId!=''">
				and create_user_id = #{createUserId}
			</if>
			<if test="name!=null and name!=''">
				and name = #{name}
			</if>
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultMap="checkSheetClassifyResultMap">
		select * from 
				check_sheet_classify a where 1=1
		<if test="pd.query_time_start!=null and pd.query_time_start!=''"><!-- 时间检索 -->
			and a.create_time &gt;= #{pd.query_time_start}
		</if>
		<if test="pd.query_time_end!=null and pd.query_time_end!=''"><!-- 时间检索 -->
			and a.create_time &lt;= #{pd.query_time_end}
		</if>	
		<if test="pd.query_key!=null and pd.query_key!=''"><!-- 关键字检索 -->
			and a.name like concat('%',#{pd.query_key},'%')
		</if>
			order by a.create_time desc	
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="String" resultMap="checkSheetClassifyResultMap">
		select * from check_sheet_classify a
	</select>
	
	<!-- 批量删除 -->
	<update id="deleteLogicalBatch" parameterType="String">
		delete from check_sheet_classify
		where 
			id in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</update>
	
	<!-- 查找含有量表的类型 -->
	<select id="listHasSheet" parameterType="String" resultType="map">
		select b.id, b.name from check_sheet a 
		left join check_sheet_classify b on a.classify = b.id 
		where a.price is not null and b.id is not null 
		group by b.id 
	</select>
	
</mapper>